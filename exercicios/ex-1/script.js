var nota1 = Number(prompt('Digite sua nota'))

var nota2 = Number(prompt('Digite novamente'))
 
var resultado = nota1 + nota2


if (resultado > 5) {
 alert('parabens voce passou')
} else {
   alert("Voce foi reprovado")
}
